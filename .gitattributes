# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts should always use LF line endings
*.sh text eol=lf

# Batch scripts use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Docker files use LF
Dockerfile text eol=lf
docker-compose*.yml text eol=lf